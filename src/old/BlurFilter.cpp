// #include "BlurFilter.h"
// #include <iostream>

// // BlurFilter class definition and logic goes here

// void BlurFilter::getUserInputAndApply(unsigned char* data, int width, int height, int channels) {
//     int kernelSize;
//     std::cout << "Enter kernel size for the blur (e.g., 3 for a 3x3 kernel): ";
//     std::cin >> kernelSize;
    
//     // Instantiate the correct blur filter based on the user's choice
//     // e.g., MedianBlur, GaussianBlur, etc.
//     // For this example, let's assume we're applying a generic blur
//     BlurFilter blur(kernelSize);
//     blur.apply(data, width, height, channels);
// }
